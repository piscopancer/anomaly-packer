// AUTO-GENERATED addon pack for `weapon_parts_overhaul` by `anomaly-packer gen-addon`. Config ltx schemas.
// Refine `TODO` (= any) by hand where the mod gave no type.
// `items/items/items_repair.ltx` is never opened directly — `items/items/base.ltx` globs it in, so these sections augment
// that file's schema. The interface is path-derived, so every mod contributing to it merges here.
import type { CommaSeparatedString } from 'anomaly-packer'

declare global {
  interface ItemsItemsBaseIni {
    tch_repair: {
      $spawn: string
      can_trade: boolean
      kind: string
      quest_item: boolean
      remove_after_use: boolean
      repair_tool: boolean
      script_binding: string
      use_condition: boolean
    }
    tch_repair0: {
      $spawn: string
      attach_angle_offset: vector
      attach_bone_name: string
      attach_position_offset: vector
      auto_attach: boolean
      cform: string
      class: string
      discovery_dependency: string
      GroupControlSection: string
      kind: string
      visual: string
    }
    sharpening_stones: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    gun_oil: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    gun_oil_ru_d: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    gun_oil_ru: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    solvent: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    cleaning_kit_p: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    cleaning_kit_s: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    cleaning_kit_r5: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    cleaning_kit_r7: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    cleaning_kit_u: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    toolkit_p: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: string
      workshop_tool: boolean
    }
    toolkit_s: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: string
      workshop_tool: boolean
    }
    toolkit_r5: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: string
      workshop_tool: boolean
    }
    toolkit_r7: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: string
      workshop_tool: boolean
    }
    glue_b: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
    }
    glue_a: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
    }
    glue_e: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
    }
    armor_repair_fa: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    sewing_kit_b: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    sewing_kit_a: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    sewing_kit_h: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    helmet_repair_kit: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: CommaSeparatedString<string[]>
      workshop_tool: boolean
    }
    light_repair_kit: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: string
      workshop_tool: boolean
    }
    medium_repair_kit: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: CommaSeparatedString<string[]>
      workshop_tool: boolean
    }
    heavy_repair_kit: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: string
      workshop_tool: boolean
    }
    exo_repair_kit: Omit<ItemsItemsBaseIni['tch_repair'], '$spawn' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      $spawn: string
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
      workshop_support: string
      workshop_tool: boolean
    }
    rasp_tool: Omit<ItemsItemsBaseIni['tch_repair'], 'attach_angle_offset' | 'attach_bone_name' | 'attach_position_offset' | 'auto_attach' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_multi' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual'> & {
      attach_angle_offset: vector
      attach_bone_name: string
      attach_position_offset: vector
      auto_attach: boolean
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_multi: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
    }
    ramrod_tool: Omit<ItemsItemsBaseIni['tch_repair'], 'attach_angle_offset' | 'attach_bone_name' | 'attach_position_offset' | 'auto_attach' | 'cost' | 'description' | 'empty_weight' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_multi' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'visual'> & {
      attach_angle_offset: vector
      attach_bone_name: string
      attach_position_offset: vector
      auto_attach: boolean
      cost: number
      description: string
      empty_weight: number
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_multi: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      visual: string
    }
    leatherman_tool: {
      $prefetch: number
      $spawn: string
      allow_repair: boolean
      attach_angle_offset: vector
      attach_bone_name: string
      attach_position_offset: vector
      auto_attach: boolean
      cform: string
      class: string
      cost: number
      degradation_factor: number
      description: string
      disassemble_tool: boolean
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      kind: string
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_multi: number
      repair_parts_sections: CommaSeparatedString<string[]>
      repair_tool: boolean
      repair_type: string
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      script_binding: string
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      use_condition: boolean
      use1_action_functor: string
      use1_allow_db: boolean
      use1_functor: string
      visual: string
    }
    sewing_thread: Omit<ItemsItemsBaseIni['tch_repair'], 'allow_repair' | 'attach_angle_offset' | 'attach_bone_name' | 'attach_position_offset' | 'auto_attach' | 'cost' | 'description' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_match' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual'> & {
      allow_repair: boolean
      attach_angle_offset: vector
      attach_bone_name: string
      attach_position_offset: vector
      auto_attach: boolean
      cost: number
      description: string
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: string
      repair_part_bonus: number
      repair_parts_match: boolean
      repair_parts_sections: string
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
    }
    heavy_sewing_thread: Omit<ItemsItemsBaseIni['tch_repair'], 'allow_repair' | 'attach_angle_offset' | 'attach_bone_name' | 'attach_position_offset' | 'auto_attach' | 'cost' | 'description' | 'inv_grid_height' | 'inv_grid_width' | 'inv_grid_x' | 'inv_grid_y' | 'inv_name' | 'inv_name_short' | 'inv_weight' | 'max_uses' | 'repair_add_condition' | 'repair_max_condition' | 'repair_min_condition' | 'repair_only' | 'repair_part_bonus' | 'repair_parts_match' | 'repair_parts_sections' | 'repair_use_actor_effects' | 'repair_use_parts' | 'tier' | 'upgr_icon_height' | 'upgr_icon_path' | 'upgr_icon_width' | 'upgr_icon_x' | 'upgr_icon_y' | 'visual' | 'workshop_support' | 'workshop_tool'> & {
      allow_repair: boolean
      attach_angle_offset: vector
      attach_bone_name: string
      attach_position_offset: vector
      auto_attach: boolean
      cost: number
      description: string
      inv_grid_height: number
      inv_grid_width: number
      inv_grid_x: number
      inv_grid_y: number
      inv_name: string
      inv_name_short: string
      inv_weight: number
      max_uses: number
      repair_add_condition: number
      repair_max_condition: number
      repair_min_condition: number
      repair_only: CommaSeparatedString<string[]>
      repair_part_bonus: number
      repair_parts_match: boolean
      repair_parts_sections: string
      repair_use_actor_effects: boolean
      repair_use_parts: boolean
      tier: number
      upgr_icon_height: number
      upgr_icon_path: string
      upgr_icon_width: number
      upgr_icon_x: number
      upgr_icon_y: number
      visual: string
      workshop_support: string
      workshop_tool: boolean
    }
  }
}
