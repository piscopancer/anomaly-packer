// AUTO-GENERATED addon pack for `3dss-for-gamma` by `anomaly-packer gen-addon`. Config ltx schemas.
// Refine `TODO` (= any) by hand where the mod gave no type.
// `items/weapons/w_vepr12.ltx` is never opened directly — `items/weapons/base.ltx` globs it in, so these sections augment
// that file's schema. The interface is path-derived, so every mod contributing to it merges here.
export {}

declare global {
  interface ItemsWeaponsBaseIni {
    wpn_vepr: {
      parent_section: string
    }
    wpn_vepr_pka: {
      parent_section: string
    }
    wpn_vepr_kobra: {
      parent_section: string
    }
    wpn_vepr_okp: {
      parent_section: string
    }
    wpn_vepr_ekp8_02: {
      parent_section: string
    }
    wpn_vepr_1p76: {
      parent_section: string
    }
    wpn_vepr_1p63: {
      parent_section: string
    }
    wpn_vepr_1p78gs: {
      parent_section: string
    }
    wpn_vepr_1pn93n2_1gs: {
      parent_section: string
    }
    wpn_vepr_1pn93: {
      parent_section: string
    }
    wpn_vepr_pso2: {
      parent_section: string
    }
    wpn_vepr_ps01: {
      parent_section: string
    }
    wpn_vepr_1p29: {
      parent_section: string
    }
    wpn_vepr_usp1: {
      parent_section: string
    }
  }
}
