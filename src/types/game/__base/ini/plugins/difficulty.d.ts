// AUTO-GENERATED by `tsx src/scripts/gen-ini-schemas.ts`. Do not edit by hand.
// Schema of `plugins\\difficulty.ltx`. Sections enumerated for autocomplete; `PluginsDifficultyIni` is a
// mergeable interface — augment it to add sections. Field value types are
// conservative (multi-token values become branded separated-strings).
export { }

declare global {
  interface PluginsDifficultyIni {
    econ_1: {
      arty_chance: number
      arty_degradation: boolean
      battery_consumption: number
      bkpk_degradation: boolean
      buy: number
      goodwill: number
      limited_bolts: boolean
      loots: number
      money_loots: number
      outfit_drops: number
      percentage_parts: boolean
      random_items: number
      repair: number
      restock: number
      rewards: number
      scope_chance: number
      sell: number
      stash_chance: number
      upgrade: number
      weapon_degradation: number
    }
    econ_2: {
      arty_chance: number
      arty_degradation: boolean
      battery_consumption: number
      bkpk_degradation: boolean
      buy: number
      goodwill: number
      limited_bolts: boolean
      loots: number
      money_loots: number
      outfit_drops: number
      percentage_parts: boolean
      random_items: number
      repair: number
      restock: number
      rewards: number
      scope_chance: number
      sell: number
      stash_chance: number
      upgrade: number
      weapon_degradation: number
    }
    econ_3: {
      arty_chance: number
      arty_degradation: boolean
      battery_consumption: number
      bkpk_degradation: boolean
      buy: number
      goodwill: number
      limited_bolts: boolean
      loots: number
      money_loots: number
      outfit_drops: number
      percentage_parts: boolean
      random_items: number
      repair: number
      restock: number
      rewards: number
      scope_chance: number
      sell: number
      stash_chance: number
      upgrade: number
      weapon_degradation: number
    }
    gdiff_1: {
      actor_immunities: number
      dispersion_base: number
      dispersion_factor: number
      hit_power: number
      max_weight: number
      notify_anomaly: boolean
      notify_geiger: boolean
      power_loss_bias: number
      radiation_day: boolean
      sleep: boolean
      thirst: boolean
    }
    gdiff_2: {
      actor_immunities: number
      dispersion_base: number
      dispersion_factor: number
      hit_power: number
      max_weight: number
      notify_anomaly: boolean
      notify_geiger: boolean
      power_loss_bias: number
      radiation_day: boolean
      sleep: boolean
      thirst: boolean
    }
    gdiff_3: {
      actor_immunities: number
      dispersion_base: number
      dispersion_factor: number
      hit_power: number
      max_weight: number
      notify_anomaly: boolean
      notify_geiger: boolean
      power_loss_bias: number
      radiation_day: boolean
      sleep: boolean
      thirst: boolean
    }
    exclude_from_multi: string[]
  }

  interface IniFileSchemas {
    'plugins\\difficulty.ltx': PluginsDifficultyIni
  }
}
